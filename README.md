# List
A very simple example of work produced from scratch with React && Next.js

## Set Up Locally
1. Fork and clone the repo
2. `cd` to your repository directory
3. Run `npm install` at the root level
4. Run `npm run dev`
5. You now have a live development server running at `localhost:8000`
6. !List!
